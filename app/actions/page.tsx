import type { Metadata } from "next"

import { ActionView } from "@/components/ip/action-view"

export const metadata: Metadata = {
  title: "미결 액션 · HADD SCIENCE 지식재산권 팔로우업",
}

export default function Page() {
  return <ActionView />
}
